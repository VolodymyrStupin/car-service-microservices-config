spring:
  config:
    activate:
      on-profile: docker
  cloud:
    config:
      uri: http://car-service-config-service:8888
      enabled: true
      
#Eureka properties
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
